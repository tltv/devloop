# Vaadin + Spring Boot app — dev loop

The instructions are shared with every agent on this repository and live in
**[AGENTS.md](AGENTS.md)**.

**Read that file now** — it is the loop (make the change, get it into the running app, verify
it in the browser), how to start and stop the app, what each kind of edit actually costs, and
how to assert that a change really rendered.
